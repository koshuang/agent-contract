# Structured Handoff Contract

Every handoff records `kind`, `status`, `scope`, `evidence`, `risks`, and `next_action`. Mutable repository, PR, CI, review, and environment state must be re-checked by the receiving agent.
